# 2026-09-25

- 为 pingfen 现场打分平台生成 `README.md`（含功能、安装、环境变量、使用流程、目录结构、API、.gitignore）。准备上传 GitHub。
- 技术栈回顾：Node + Express + ws + better-sqlite3 + qrcode + docx；四端 /screen /admin /judge /control；双轨评分、一人一码、Word 二维码导出、重置评分。

## 部署相关（下午）
- 用户考虑 Render 与 Replit 两种部署。已为两者做准备：
  - `server.js` 的 baseUrl 支持 `BASE_URL`(手动) > `REPLIT_DOMAINS`(自动) > 局域网 IP 回退，确保扫码二维码指向公网。
  - `package.json` 把 `playwright` 移到 devDependencies（避免部署下载 Chromium）。
  - README 新增 Render / Replit 两节。
- 关键结论：**Replit 休眠不会清空 SQLite**（持久盘）；Render 免费版重新部署会重置临时盘，比赛当天部署当天用才安全。WAL 模式在硬杀进程后可自动恢复。
